


  function setup() {
    createCanvas( windowWidth, windowHeight );

}

function draw() {
  background( 'rgb(255, 210, 94)' );
    noCursor();
    fill( 140 );
    ellipse( mouseX, mouseY, 10 );
}
